{% extends 'base.html.twig' %}

{% block title %}HistoryCustomers{% endblock %}

{% block body %}
    <h1>HistoryCustomers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ history_customer.id }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ history_customer.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_history_customers_index') }}">back to list</a>

    <a href="{{ path('app_history_customers_edit', {'id': history_customer.id}) }}">edit</a>

    {{ include('history_customers/_delete_form.html.twig') }}
{% endblock %}
