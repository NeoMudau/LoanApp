<form method="post" action="{{ path('app_history_customers_delete', {'id': history_customer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ history_customer.id) }}">
    <button class="btn">Delete</button>
</form>
